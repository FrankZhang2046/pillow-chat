OPENROUTER_API_KEY=sk-or-v1-...
DEFAULT_MODEL=anthracite-org/magnum-v4-72b
DATABASE_URL=postgres://postgres:dev@localhost:5432/postgres
SESSION_SECRET=generate-with-openssl-rand-hex-32
# RATE_LIMIT_PER_SESSION=50
# RATE_LIMIT_PER_IP_HOURLY=200
# Set to enable the /api/admin?token=<value> backdoor that bypasses rate limits.
# Leave empty/unset to disable entirely. Generate with: openssl rand -hex 32
# ADMIN_TOKEN=
